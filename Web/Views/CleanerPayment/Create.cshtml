@using Web.Models;
@model CleanerPaymentCreateViewModel

@{
    ViewBag.Title = "Cleaner Payment Create";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("Create", "CleanerPayment", FormMethod.Post))
{
    @Html.HiddenFor(a => a.CleanerPaymentID)

    <div class="row">
        <div class="col-md-3">
            @Html.DisplayNameFor(a => a.ProofImage)
        </div>
        <div class="col-md-3">
            @Html.EditorFor(a => a.ProofImage)
        </div>
        <div class="col-md-3">
            @Html.DisplayNameFor(a => a.Remark)
        </div>
        <div class="col-md-3">
            @Html.TextAreaFor(a => a.Remark)
        </div>
    </div>

    <div class="row">
        <div class="col-md-12 right">
            <input type="submit" value="Create" class="btn btn-default" />
            <a class="btn btn-warning" href="@Url.Action("Index", "CleanerPayment")" role="button">Back</a>
        </div>
    </div>
}